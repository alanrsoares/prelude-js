import filter from './filter.js'
import id from '../General/id.js'

/**
 * @remarks
 *
 * ```text
 * filter :: [a] -> [a]
 * ```
 *
 * @param arg1 - `[a]`
 *
 * @returns `[a]`
 */
export default filter(id)
