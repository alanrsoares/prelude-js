import scan from './scan.js'

/**
 * @remarks
 *
 * ```text
 * scanl :: (a -> b) -> [a] -> [b]
 * ```
 *
 * @param arg1 - `(a -> b)`
 * @param arg2 - `[a]`
 *
 * @returns `[b]`
 */
export default scan
