import head from './head.js'

/**
 * Returns the first item of the list, or undefined. Alias of {@link head}.
 *
 * @example
 * ```ts
 * first([1, 2, 3]) //=> 1
 * ```
 */
const first = head

export default first
