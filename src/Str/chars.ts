/**
 * Splits a string into an array of its characters.
 *
 * @example
 * ```ts
 * chars('abc') //=> ['a', 'b', 'c']
 * ```
 */
const chars = (value: string): string[] => value.split('')

export default chars
