// Shared fixtures for homepage snippet modules. Anything imported from here is
// kept for type-checking but hidden (cut) from the rendered snippet. Empty for
// now — the demos are self-contained.
export {}
