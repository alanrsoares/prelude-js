---
title: "List API"
description: "Complete API reference for preludejs/List"
---

# List API Reference

## all <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
all :: (a -> Boolean) -> [a] -> Boolean
```

#### TypeScript Signature

```typescript
declare const all: <A>(fn: Predicate<A>, xs: readonly A[]) => boolean
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Boolean)` |
| `arg2` | `[a]` |

#### Returns

`Boolean`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## and <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
and :: [a] -> Boolean
```

#### TypeScript Signature

```typescript
declare const and: (xs: readonly unknown[]) => boolean
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`Boolean`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## any <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
any :: (a -> Boolean) -> [a] -> Boolean
```

#### TypeScript Signature

```typescript
declare const any: <A>(fn: Predicate<A>, xs: readonly A[]) => boolean
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Boolean)` |
| `arg2` | `[a]` |

#### Returns

`Boolean`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## at <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
at :: Number -> [a] -> a
```

#### TypeScript Signature

```typescript
declare const at: <A>(index: number, xs: readonly A[]) => A | undefined
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `Number` |
| `arg2` | `[a]` |

#### Returns

`a`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## breakList <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
breakList :: (a -> Bool) -> [a] -> [[a], [a]]
```

#### TypeScript Signature

```typescript
declare const breakList: <A>(fn: Predicate<A>, xs: readonly A[]) => [A[], A[]]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Bool)` |
| `arg2` | `[a]` |

#### Returns

`[[a], [a]]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## compact <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
filter :: [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const compact: <A>(xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## concat <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
concat :: [[a]] -> [a]
```

#### TypeScript Signature

```typescript
declare const concat: <A>(xss: readonly (readonly A[])[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[[a]]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## concatMap <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
concatMap :: (a -> [b]) -> [a] -> [b]
```

#### TypeScript Signature

```typescript
declare const concatMap: <A, B>(fn: Mapper<A, readonly B[]>, xs: readonly A[]) => B[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> [b])` |
| `arg2` | `[a]` |

#### Returns

`[b]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## countBy <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
countBy :: (a -> b) -> [a] -> { b: Number }
```

#### TypeScript Signature

```typescript
declare const countBy: <A, B>(fn: Accessor<A, B>, xs: readonly A[]) => Record<string, number>
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`{ b: Number }`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## difference <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
difference :: ([a], [a], ...) -> [a]
```

#### TypeScript Signature

```typescript
declare const difference: <A>(left: readonly A[], right: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `([a], [a], ...)` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## drop <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
drop :: Number -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const drop: <A>(count: number, xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `Number` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## dropWhile <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
dropWhile :: (a -> Bool) -> [x] -> [x]
```

#### TypeScript Signature

```typescript
declare const dropWhile: <A>(fn: Predicate<A>, xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Bool)` |
| `arg2` | `[x]` |

#### Returns

`[x]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## each <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

Applies a function to each item in the list and returns the original list. Used for side effects.

```haskell
each :: (a -> b) -> [a] -> void
```

#### TypeScript Signature

```typescript
declare const each: <A>(fn: (value: A, index: number, array: readonly A[]) => unknown, xs: readonly A[]) => void
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`void`

#### Example

```javascript
each((x) => x.push('boom'), [['a'], ['b'], ['c']]);
//=> [['a', 'boom'], ['b', 'boom'], ['c', 'boom']]
```

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## elem <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

Checks whether a list contains a given value.

```haskell
elem :: a -> [a] -> Boolean
```

#### TypeScript Signature

```typescript
function elem<A>(value: A): (xs: readonly A[]) => boolean
function elem<A>(value: A, xs: readonly A[]): boolean
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `a` |
| `arg2` | `[a]` |

#### Returns

`Boolean`

#### Example

```javascript
elem(3, [1, 2, 3]); //=> true
```

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## elemIndex <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
elemIndex :: a -> [a] -> Number
```

#### TypeScript Signature

```typescript
declare const elemIndex: <A>(value: A, xs: readonly A[]) => number
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `a` |
| `arg2` | `[a]` |

#### Returns

`Number`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## elemIndices <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
elemIndices :: a -> [a] -> [Number]
```

#### TypeScript Signature

```typescript
declare const elemIndices: <A>(value: A, xs: readonly A[]) => number[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `a` |
| `arg2` | `[a]` |

#### Returns

`[Number]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## empty <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
empty :: [a] -> Boolean
```

#### TypeScript Signature

```typescript
declare const empty: <A>(xs: readonly A[]) => boolean
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`Boolean`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## filter <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
filter :: (a -> Boolean) -> [a] -> [a]
```

#### TypeScript Signature

```typescript
function filter<A>(fn: Predicate<A>): (xs: readonly A[]) => A[]
function filter<A>(fn: Predicate<A>, xs: readonly A[]): A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Boolean)` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## find <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
find :: (a -> Boolean) -> [a] -> a
```

#### TypeScript Signature

```typescript
declare const find: <A>(fn: Predicate<A>, xs: readonly A[]) => A | undefined
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Boolean)` |
| `arg2` | `[a]` |

#### Returns

`a`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## findIndex <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
findIndex :: (a -> Boolean) -> [a] -> Number
```

#### TypeScript Signature

```typescript
declare const findIndex: <A>(fn: Predicate<A>, xs: readonly A[]) => number
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Boolean)` |
| `arg2` | `[a]` |

#### Returns

`Number`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## findIndices <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
findIndices :: (a -> Boolean) -> [a] -> [Number]
```

#### TypeScript Signature

```typescript
declare const findIndices: <A>(fn: Predicate<A>, xs: readonly A[]) => number[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Boolean)` |
| `arg2` | `[a]` |

#### Returns

`[Number]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## first <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
first :: [a] -> a
```

#### TypeScript Signature

```typescript
declare const first: <A>(xs: readonly A[]) => A | undefined
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`a`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## flatten <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
flatten :: List -> List
```

#### TypeScript Signature

```typescript
declare const flatten: <A>(xss: readonly (readonly A[])[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `List` |

#### Returns

`List`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## fold <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
fold :: (b -> a -> b) -> b -> [a] -> b
```

#### TypeScript Signature

```typescript
declare const fold: <A, B>(fn: Reducer<A, B>, initial: A, xs: readonly B[]) => A
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(b -> a -> b)` |
| `arg2` | `b` |
| `arg3` | `[a]` |

#### Returns

`b`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## fold1 <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
fold1 :: (a -> a -> a) -> [a] -> a
```

#### TypeScript Signature

```typescript
declare const fold1: <A>(fn: (acc: A, value: A) => A, xs: readonly A[]) => A
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> a -> a)` |
| `arg2` | `[a]` |

#### Returns

`a`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## foldl <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
foldl :: (b -> a -> b) -> b -> [a] -> b
```

#### TypeScript Signature

```typescript
declare const foldl: <A, B>(fn: Reducer<A, B>, initial: A, xs: readonly B[]) => A
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(b -> a -> b)` |
| `arg2` | `b` |
| `arg3` | `[a]` |

#### Returns

`b`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## foldl1 <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
foldl1 :: (a -> a -> a) -> [a] -> a
```

#### TypeScript Signature

```typescript
declare const foldl1: <A>(fn: (acc: A, value: A) => A, xs: readonly A[]) => A
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> a -> a)` |
| `arg2` | `[a]` |

#### Returns

`a`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## foldr <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
foldr :: (b -> a -> b) -> b -> [a] -> b
```

#### TypeScript Signature

```typescript
declare const foldr: <A, B>(
fn: (value: B, acc: A, index: number, array: readonly B[]) => A,
initial: A,
xs: readonly B[],
) => A
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(b -> a -> b)` |
| `arg2` | `b` |
| `arg3` | `[a]` |

#### Returns

`b`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## foldr1 <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
foldr1 :: (a -> a -> a) -> [a] -> a
```

#### TypeScript Signature

```typescript
declare const foldr1: <A>(fn: (value: A, acc: A) => A, xs: readonly A[]) => A
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> a -> a)` |
| `arg2` | `[a]` |

#### Returns

`a`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## groupBy <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
groupBy :: (a -> b) -> [a] -> { b: [b] }
```

#### TypeScript Signature

```typescript
declare const groupBy: <A>(fn: Predicate<A>, xs: readonly A[]) => A[][]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`{ b: [b] }`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## head <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
head :: [a] -> a
```

#### TypeScript Signature

```typescript
declare const head: <A>(xs: readonly A[]) => A | undefined
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`a`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## initial <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
initial :: [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const initial: <A>(xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## intersection <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
intersection :: ([a], [a], ...) -> [a]
```

#### TypeScript Signature

```typescript
declare const intersection: <A>(left: readonly A[], right: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `([a], [a], ...)` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## last <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
last :: [a] -> a
```

#### TypeScript Signature

```typescript
declare const last: <A>(xs: readonly A[]) => A | undefined
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`a`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## length <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

Returns the number of items in a list.

```haskell
length :: [a] -> Number
```

#### TypeScript Signature

```typescript
function length<A>(xs: readonly A[]): number
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`Number`

#### Example

```javascript
length([1, 2, 3]); //=> 3
```

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## map <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

Applies a function to each item in the list, and produces a new list with the results. The length of the result is the same length as the input.

```haskell
map :: (a -> b) -> [a] -> [b]
```

#### TypeScript Signature

```typescript
function map<A, B>(fn: Mapper<A, B>): (xs: readonly A[]) => B[]
function map<A, B>(fn: Mapper<A, B>, xs: readonly A[]): B[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`[b]`

#### Example

```javascript
map((x) => x + 1, [1, 2, 3]);
//=> [2, 3, 4]
```

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## maximum <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
maximum :: [Number] -> Number
```

#### TypeScript Signature

```typescript
declare const maximum: <A>(xs: readonly A[]) => A | undefined
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[Number]` |

#### Returns

`Number`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## maximumBy <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
maximunBy :: (a -> b) -> [a] -> b
```

#### TypeScript Signature

```typescript
declare const maximumBy: <A, B>(fn: Accessor<A, B>, xs: readonly A[]) => A | undefined
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`b`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## mean <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
mean :: [Number] -> Number
```

#### TypeScript Signature

```typescript
declare const mean: (xs: readonly number[]) => number
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[Number]` |

#### Returns

`Number`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## minimum <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
minimum :: [Number] -> Number
```

#### TypeScript Signature

```typescript
declare const minimum: <A>(xs: readonly A[]) => A | undefined
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[Number]` |

#### Returns

`Number`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## minimumBy <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
minimumBy :: (a -> b) -> [a] -> b
```

#### TypeScript Signature

```typescript
declare const minimumBy: <A, B>(fn: Accessor<A, B>, xs: readonly A[]) => A | undefined
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`b`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## notElem <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

Checks whether a list does not contain a given value.

```haskell
notElem :: a -> [a] -> Boolean
```

#### TypeScript Signature

```typescript
function notElem<A>(value: A): (xs: readonly A[]) => boolean
function notElem<A>(value: A, xs: readonly A[]): boolean
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `a` |
| `arg2` | `[a]` |

#### Returns

`Boolean`

#### Example

```javascript
notElem(4, [1, 2, 3]); //=> true
```

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## or <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
or :: [a] -> Boolean
```

#### TypeScript Signature

```typescript
declare const or: (xs: readonly unknown[]) => boolean
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`Boolean`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## partition <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
partition :: (a -> Boolean) -> [a] -> [[a] [a]]
```

#### TypeScript Signature

```typescript
declare const partition: <A>(fn: Predicate<A>, xs: readonly A[]) => [A[], A[]]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Boolean)` |
| `arg2` | `[a]` |

#### Returns

`[[a] [a]]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## product <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
product :: [Number] -> Number
```

#### TypeScript Signature

```typescript
declare const product: (xs: readonly number[]) => number
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[Number]` |

#### Returns

`Number`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## range <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
range :: (Number, Number?, Number?) -> [Number]
```

#### TypeScript Signature

```typescript
function range(to: number, from?: number, step?: number): number[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(Number, Number?, Number?)` |

#### Returns

`[Number]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## reduce <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
reduce :: ((a, b) -> a) -> [b] -> a
```

#### TypeScript Signature

```typescript
function reduce<A, B>(fn: Reducer<A, B>): (initial: A) => (xs: readonly B[]) => A
function reduce<A, B>(fn: Reducer<A, B>, initial: A): (xs: readonly B[]) => A
function reduce<A, B>(fn: Reducer<A, B>, initial: A, xs: readonly B[]): A
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `((a, b) -> a)` |
| `arg2` | `[b]` |

#### Returns

`a`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## reject <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
reject :: (a -> Boolean) -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const reject: <A>(fn: Predicate<A>, xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Boolean)` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## reverse <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
reverse :: [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const reverse: <A>(xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## scan <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
scan :: (a -> b) -> [a] -> [b]
```

#### TypeScript Signature

```typescript
declare const scan: <A, B>(fn: Reducer<A, B>, initial: A, xs: readonly B[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`[b]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## scan1 <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
scan1 :: (a -> a -> a) -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const scan1: <A, B>(fn: Reducer<A, B>, initial: A, xs: readonly B[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> a -> a)` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## scanl <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
scanl :: (a -> b) -> [a] -> [b]
```

#### TypeScript Signature

```typescript
declare const scanl: <A, B>(fn: Reducer<A, B>, initial: A, xs: readonly B[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`[b]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## scanl1 <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
scanl1 :: (a -> a -> a) -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const scanl1: <A, B>(fn: Reducer<A, B>, initial: A, xs: readonly B[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> a -> a)` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## scanr <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
scanr :: (a -> b -> b) -> b -> [a] -> [b]
```

#### TypeScript Signature

```typescript
declare const scanr: <A, B>(
fn: (value: B, acc: A, index: number, array: readonly B[]) => A,
initial: A,
xs: readonly B[],
) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b -> b)` |
| `arg2` | `b` |
| `arg3` | `[a]` |

#### Returns

`[b]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## scanr1 <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
scanr1 :: (a -> a -> a) -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const scanr1: <A, B>(
fn: (value: B, acc: A, index: number, array: readonly B[]) => A,
initial: A,
xs: readonly B[],
) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> a -> a)` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## slice <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
slice :: Number -> Number -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const slice: <A>(start: number, end: number, xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `Number` |
| `arg2` | `Number` |
| `arg3` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## sort <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
sort :: List -> List
```

#### TypeScript Signature

```typescript
declare const sort: <A>(xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `List` |

#### Returns

`List`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## sortBy <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
sortBy :: (a -> b) -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const sortBy: <A, B>(fn: Accessor<A, B>, xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## sortWith <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
sortWith :: (a -> Number) -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const sortWith: <A>(fn: Comparer<A>, xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Number)` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## span <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
span :: (a -> Bool) -> [a] -> [[a], [a]]
```

#### TypeScript Signature

```typescript
declare const span: <A>(fn: Predicate<A>, xs: readonly A[]) => [A[], A[]]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Bool)` |
| `arg2` | `[a]` |

#### Returns

`[[a], [a]]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## splitAt <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
splitAt :: Number -> [a] -> [[a], [a]]
```

#### TypeScript Signature

```typescript
declare const splitAt: <A>(count: number, xs: readonly A[]) => [A[], A[]]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `Number` |
| `arg2` | `[a]` |

#### Returns

`[[a], [a]]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## sum <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
sum :: [Number] -> Number
```

#### TypeScript Signature

```typescript
declare const sum: (xs: readonly number[]) => number
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[Number]` |

#### Returns

`Number`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## tail <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
tail :: [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const tail: <A>(xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## take <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
take :: Number -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const take: <A>(count: number, xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `Number` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## takeWhile <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
takeWhile :: (a -> Bool) -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const takeWhile: <A>(fn: Predicate<A>, xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> Bool)` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## unfoldr <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
unfoldr :: (a -> [b]) -> [a] -> [b]
```

#### TypeScript Signature

```typescript
declare const unfoldr: <A, B>(fn: (seed: B) => [A, B] | undefined, seed: B) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> [b])` |
| `arg2` | `[a]` |

#### Returns

`[b]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## union <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
union :: ([a], [a], ...) -> [a]
```

#### TypeScript Signature

```typescript
declare const union: <A>(left: readonly A[], right: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `([a], [a], ...)` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## unique <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
unique :: [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const unique: <A>(xs: readonly A[]) => A[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## uniqueBy <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
uniqueBy :: (a -> b) -> [a] -> [a]
```

#### TypeScript Signature

```typescript
declare const uniqueBy: <A, B>(fn: Accessor<A, B>, xs: readonly A[]) => B[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b)` |
| `arg2` | `[a]` |

#### Returns

`[a]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## zip <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
zip :: [a] -> [b] -> [[a, b]]
```

#### TypeScript Signature

```typescript
declare const zip: <A, B>(xs: readonly A[], ys: readonly B[]) => Array<Pair<A, B>>
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |
| `arg2` | `[b]` |

#### Returns

`[[a, b]]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## zipAll <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
zipAll :: [a] -> [b] -> [[a, b]]
```

#### TypeScript Signature

```typescript
declare const zipAll: <A, B>(xs: readonly A[], ys: readonly B[]) => Array<Pair<A | undefined, B | undefined>>
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `[a]` |
| `arg2` | `[b]` |

#### Returns

`[[a, b]]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## zipAllWith <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
zipAllWith :: (a -> b -> c) -> [a] -> [b] -> [c]
```

#### TypeScript Signature

```typescript
declare const zipAllWith: <A, B, C>(
fn: (left: A | undefined, right: B | undefined) => C,
xs: readonly A[],
ys: readonly B[],
) => C[]
```

#### Parameters

| Parameter | Type |
|---|---|
| `arg1` | `(a -> b -> c)` |
| `arg2` | `[a]` |
| `arg3` | `[b]` |

#### Returns

`[c]`

<hr className="my-8 border-neutral-200 dark:border-neutral-800" />

## zipWith <span className="inline-flex items-center rounded-full border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-950/30 text-emerald-600 dark:text-emerald-400 text-xs font-semibold px-2.5 py-0.5 font-medium ml-2 align-middle">ƒ</span>

```haskell
zipWith ::
```

#### TypeScript Signature

```typescript
function zipWith<A, B, C>(fn: (left: A, right: B) => C): (xs: readonly A[]) => (ys: readonly B[]) => C[]
function zipWith<A, B, C>(fn: (left: A, right: B) => C, xs: readonly A[]): (ys: readonly B[]) => C[]
function zipWith<A, B, C>(fn: (left: A, right: B) => C, xs: readonly A[], ys: readonly B[]): C[]
```

